<%= semantic_form_for @introduction, :html => { :class => 'form-horizontal' } do |f| %>
   <%= f.inputs do%>
     <%= f.input :title %>
     <%= f.input :content, as: :ckeditor%>
   <% end %>
   <%= f.actions%>
  

  
<% end %>
